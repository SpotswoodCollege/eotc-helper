%h1 Activities
- if can? :create, Activity
  = link_to t('buttons.activity.new'), new_activity_path
%ul
  - @activities.each do |activity|
    %li
      = link_to activity.name, activity_path(activity)
